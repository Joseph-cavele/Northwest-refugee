# Dev requests go through the Vite proxy to http://localhost:5000, so this is left blank
# locally and set to the API's origin in a deployed build.
VITE_API_BASE_URL=

# The public help widget calls /api/v1/guide, which needs no key and no login.
